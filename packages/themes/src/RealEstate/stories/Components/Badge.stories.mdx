import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import Badge from '@pagerland/common/src/components/Badge';
import Icon from '@pagerland/common/src/components/Icon';
import TagAlt from '@pagerland/icons/src/line/TagAlt';

import Theme from '../../index';

<Meta title="Themes/RealEstate/Components/Badge" component={Badge} />

# Badge

Badges contain content and actions about a single subject.

```js
import Badge from '@pagerland/common/src/components/Badge';

<Badge>Content of the badge</Badge>;
```

<Preview>
  <Story name="Default">
    <Theme>
      <Badge>
        <Icon icon={TagAlt} />
        $3,450,000
      </Badge>
    </Theme>
  </Story>
</Preview>

## Props

This component doesn't have specified prop types. Use default props as in SPAN html tag and styled system stuff.
