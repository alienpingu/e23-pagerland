import { Meta, Story, Props, Preview } from '@storybook/addon-docs/blocks';

import Theme from '../../index';
import Checkbox from '@pagerland/common/src/components/Checkbox';

<Meta title="Themes/RealEstate/Form/Checkbox" component={Checkbox} />

# Checkbox

Checkbox buttons allow the user to select one option from a set.

```js
import Checkbox from '@pagerland/common/src/components/Checkbox';

<Checkbox />;
```

### Default

<Preview>
  <Story name="Default">
    <Theme>
      <Checkbox checked={false} label="Checkboxbutton label" onToggle={() => {}} />
    </Theme>
  </Story>
</Preview>

### Checked

<Preview>
  <Story name="Checked">
    <Theme>
      <Checkbox checked={true} label="Checkboxbutton label" onToggle={() => {}} />
    </Theme>
  </Story>
</Preview>

### With error

<Preview>
  <Story name="With error">
    <Theme>
      <Checkbox
        checked={false}
        error="Error message"
        label="Checkboxbutton label"
        onToggle={() => {}}
      />
    </Theme>
  </Story>
</Preview>

### Custom color

<Preview>
  <Story name="Custom color">
    <Theme>
      <Checkbox
        checked={false}
        IndicatorProps={{ color: '#56E39F' }}
        label="Checkboxbutton label"
        onToggle={() => {}}
      />
      <Checkbox
        checked={true}
        IndicatorProps={{ color: '#56E39F' }}
        label="Checkboxbutton label"
        onToggle={() => {}}
      />
    </Theme>
  </Story>
</Preview>

## Props

<Props of={Checkbox} />
